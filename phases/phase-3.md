# Phase 3 - Analytics (Umami)

> Part of the [Portfolio Fullstack Monorepo](../MAIN_PLAN.md) plan.

- Run self-hosted Umami as a container using its own database on the shared Postgres instance.
- Add the Umami tracking script to `apps/web` for pageviews/sessions; create a site in the Umami dashboard.
